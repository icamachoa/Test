//[session.db|Untyped,session.idusuario|Untyped,]
SELECT IDDISTRIBUIDOR FROM <#SESSION.DB/>.DBO.EMPRESAS WHERE IDEMPRESA = (SELECT IDEMPRESA FROM <#SESSION.DB/>.DBO.USUARIOS WHERE IDUSUARIO = <#SESSION.IDUSUARIO/> )